# image_root: '/export/share/datasets/vision/coco/images/'
# ann_root: 'annotation'
dataset: 'coco'
image_root: '/home/minwoo/Distillation_Project/datasets/vision/coco/images/'
ann_root: '/home/minwoo/Distillation_Project/datasets/vision/coco/annotations/'
coco_gt_root: '/home/minwoo/Distillation_Project/datasets/vision/coco/annotation/coco_gt'
# set pretrained as a file path or an url
# pretrained: '/home/minwoo/Distillation_Project/output/pretrain_coco_only_small/checkpoint_09.pth'
pretrained: '/home/minwoo/Distillation_Project/output/oficial_models/pretrained/model_base_14M.pth'
output_dir: 'output/Retrieval_coco'

# size of vit model; base or large
# vit: 'small'
# batch_size_train: 32
# batch_size_test: 64
# vit_grad_ckpt: True
# vit_ckpt_layer: 4
# init_lr: 1e-5

vit: 'base'
my_bert_size: 'base' # 추가
batch_size_train: 32
batch_size_test: 64
vit_grad_ckpt: True
vit_ckpt_layer: 4
init_lr: 1.0e-5

# vit: 'large'
# batch_size_train: 16
# batch_size_test: 32
# vit_grad_ckpt: True
# vit_ckpt_layer: 12
# init_lr: 5e-6

# image_size: 384 # finetune opt
image_size: 224 # for zero shot opt
queue_size: 57600
alpha: 0.4
k_test: 256
negative_all_rank: True

# optimizer
weight_decay: 0.05
min_lr: 0
max_epoch: 6

